spring:
  datasource:
    driverClassName: org.h2.Driver
    password: password
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  redis:
    host: localhost
    port: 6379
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: f299099dc6b0a8e6e1db
            client-secret: 21b9bd8569d8486497dbff78d513e81802265e79
          google:
            client-id: google-client-id
            client-secret: google-client-secret